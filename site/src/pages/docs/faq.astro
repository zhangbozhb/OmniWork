---
import DocsNav from "@/components/DocsNav.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
---

<BaseLayout title="FAQ" description="Frequently asked questions for OmniWork public users.">
  <div class="container doc-layout">
    <DocsNav />
    <article class="prose">
      <p class="eyebrow">FAQ</p>
      <h1>常见问题</h1>

      <h2>Public Web 和 Web App 有什么区别？</h2>
      <p>
        Public Web 是官网、文档和下载入口，占用 `/`、`/docs/` 和 `/download/`。
        Web App 是产品客户端，占用 `/app/`，负责配对、查看 Workspace 和进入终端。
      </p>

      <h2>当前能从 App Store 下载吗？</h2>
      <p>
        当前仓库没有能够验证的 App Store 链接。下载页不会展示占位地址；可以按仓库文档配置签名后从源码构建。
      </p>

      <h2>Android APK 从哪里获取？</h2>
      <p>
        当前没有已验证的公开 APK。发布清单只有在资产真实存在并生成 SHA256 后才会展示下载入口。
      </p>

      <h2>桌面端 Agent 和 Relay 如何安装？</h2>
      <p>
        两者均已通过 npm 发布，要求 Node.js 22.6 或更高版本；桌面端 Agent 还要求安装 tmux。
      </p>

      <h2>Web App 是否支持二维码扫描？</h2>
      <p>
        Web App 不使用摄像头扫描。你可以手动输入 Relay URL、Agent device ID 和可选名称，或使用配对链接导入。App 身份、公钥和设备信息由客户端自动提供。
      </p>

      <h2>为什么新 Agent 启动后没有立即连接？</h2>
      <p>
        Relay 默认采用人工 Agent 授权。新 Agent 完成签名身份验证后会进入 Relay Admin 待审批列表；批准后 Agent 会通过后台重试自动连接。运维也可启用自动模式，但 device ID 与来源 IP 封禁规则仍会优先拒绝连接。
      </p>

      <h2>为什么这里没有 Admin、metrics 或 debug 说明？</h2>
      <p>
        这些属于内部运维和受控管理能力，不应暴露给公网访客。Public Web 只保留普通用户需要的公开信息。
      </p>

      <h2>Agent 消息会发送系统推送吗？</h2>
      <p>
        当前实现的是 App 内本地收件箱和通知偏好，尚未接入 APNs/FCM 系统推送。
      </p>
    </article>
  </div>
</BaseLayout>
