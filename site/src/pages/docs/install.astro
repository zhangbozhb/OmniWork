---
import DocsNav from "@/components/DocsNav.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
---

<BaseLayout title="安装指南" description="Install OmniWork clients and Desktop Agent.">
  <div class="container doc-layout">
    <DocsNav />
    <article class="prose">
      <p class="eyebrow">Install</p>
      <h1>安装指南</h1>

      <h2>iOS</h2>
      <p>
        仓库支持 React Native iOS 源码构建和签名。当前下载清单没有可验证的 App Store
        或公开 IPA 入口；IPA 只有在满足 Ad Hoc、企业签名、开发测试或 MDM 条件时才能分发。
      </p>

      <h2>Android</h2>
      <p>
        仓库支持从源码构建 APK 和 AAB。下载页只会展示发布清单中存在且带 SHA256
        的 APK；当前没有已验证的公开 APK 入口。
      </p>

      <h2>桌面端 Agent</h2>
      <p>
        桌面端 Agent 要求 Node.js 22.6 或更高版本和 tmux，通过 npm 安装：
      </p>
      <pre><code>npm install --global @omni-work/desktop-agent
omniwork-agent --config /path/to/config.yml</code></pre>
      <p>它负责发现工作区、连接 tmux sessions，并提供 Workspace、Git、Files、Terminal 和 Agent 消息能力。</p>

      <h2>Relay Server</h2>
      <p>自托管 Relay 同样通过 npm 安装：</p>
      <pre><code>npm install --global @omni-work/relay-server
omniwork-relay --config /path/to/config.yml</code></pre>

      <h2>Web App</h2>
      <p>
        Web App 入口是 <a href="/app/">/app/</a>。它复用产品 Web SPA，不属于 Public Web 本身。
        Web 配对不使用摄像头扫描，需要手动输入配对信息或打开配对链接。
      </p>

      <h2>历史版本</h2>
      <p>
        npm 包以 <a href="https://www.npmjs.com/org/omni-work">npm organization</a> 为准；
        真实存在的安装资产、release notes 和校验文件以
        <a href="https://github.com/zhangbozhb/OmniWork/releases">GitHub Releases</a> 为准。
      </p>
    </article>
  </div>
</BaseLayout>
