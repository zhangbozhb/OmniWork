---
import DocsNav from "@/components/DocsNav.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
---

<BaseLayout title="项目概览" description="OmniWork project overview.">
  <div class="container doc-layout">
    <DocsNav />
    <article class="prose">
      <p class="eyebrow">Overview</p>
      <h1>项目概览</h1>
      <p class="overview-lede">
        OmniWork 不仅仅是一个终端，它是你随时随地驾驭本地 Agent 的专属控制台。我们将电脑上的强大操作体验，平滑且安全地延伸至你的 iOS、Android 与浏览器端，让你不再受限于物理设备。
      </p>

      <h2>我们解决的核心痛点</h2>
      <p>
        传统的 Agent 命令行工具通常局限于本地终端——开发者必须守在电脑前才能查看会话、审查代码差异（Diff）或输入指令。
        OmniWork 拒绝这种低效模式。我们将这些高频交互转化为跨设备的操作界面，同时坚守电脑本机作为实际执行环境的绝对安全原则。
      </p>

      <h2>系统架构组成</h2>
      <ul>
        <li><strong>App / Web 客户端</strong>：承担设备配对、环境选择，并提供 Workspace、终端会话、Codex 结构化会话、Agent 消息、Git 与文件界面。</li>
        <li><strong>桌面端 Agent</strong>：作为运行在电脑本地的 Node.js 进程，负责发现工作区、管理 tmux 会话、对接配置化 Provider，并执行文件与 Git 操作。</li>
        <li><strong>Relay 中继服务</strong>：在公网或企业内网中提供稳定的连接中转，协助客户端与桌面端 Agent 顺利建立加密通道。</li>
        <li><strong>共享协议层</strong>：定义了跨端消息、身份认证、会话状态、终端交互、文件传输及网络升级链路的标准数据结构。</li>
        <li><strong>公开站点 (Public Web)</strong>：即您当前访问的站点，专门用于项目介绍、公开文档查阅、应用下载及 Web 客户端的访问入口引导。</li>
      </ul>

      <h2>工作运行机制</h2>
      <p>
        用户首先在电脑上启动 Agent，获得 Relay URL 与由 Agent 公钥派生的 Device ID。随后，用户在 iOS、Android 或 Web 客户端输入这些目标信息，或扫描、粘贴同内容的配对链接。
        新 Agent 默认先由 Relay 管理员批准，也可在未命中 device/IP 封禁时自动授权。客户端会自动生成并复用自己的 App 身份，通过 Relay 获取在线 Agent 的公钥并校验 Device ID 绑定；Desktop Agent 默认还要求在本机 Agent Admin 批准新 App，显式自动模式则在签名和 scope 校验后授权。随后双方建立端到端加密连接，并可在条件允许时升级为 P2P 直连。用户即可选定工作区，在统一上下文中管理会话、审阅 Git 变更、浏览项目文件并接管终端交互。
      </p>
      <p>
        Codex、Claude Code 与 TraeX 可创建结构化会话，在 App 中提交 prompt、查看对话与活动事件，并处理受支持的审批与 Agent 提问。Codex、Claude Code、Trae
        与 Trae CN hooks 也可产生 App 内消息；平台原生系统 Push 尚未接入，部分 Provider 的完整 Diff 详情仍取决于对应结构化协议能力。
      </p>

      <h2>适用人群</h2>
      <ul>
        <li>频繁使用 Codex、Claude、Gemini、Trae、Trae CN 或自定义命令行 Provider 的开发者。</li>
        <li>希望在离开电脑屏幕后，依然能够随时查看并接续本地 Agent 任务的用户。</li>
        <li>期望在移动端快速审阅会话状态、代码 Diff、文件内容及终端输出的早期采用者。</li>
      </ul>
    </article>
  </div>
</BaseLayout>

<style>
  .overview-lede {
    font-size: 18px;
    line-height: 1.7;
    margin-bottom: 2rem;
  }
</style>
